import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  scrollViewContentContainer: {
    alignItems: "center",
    paddingVertical: 16,
  },
  modal: {
    marginHorizontal: "5%",
  },
});
