export interface ICustomer {
  customerId: string;
  providerId: string;
  businessId: string;
  requestConfirmed: boolean;
  name: string;
  profilePictureUrl: string;
  description: string;
  createdAt: string;
  modifiedAt: string;
}
