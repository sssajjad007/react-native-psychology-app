export interface IProvider {
  id: string;
  name: string;
  profilePictureUrl: string;
  description: string;
}
export interface ICustomer {
  id: string;
  // role: string;
  // username: string;
  firstName: string;
  lastName: string;
  description: string;
  // gender: "male" | "female";
  // birthday: string;
  profilePictureUrl: string;
  // address: string;
  // telephone: number;
  // createdAt: string;
  // modifiedAt: string;
}
