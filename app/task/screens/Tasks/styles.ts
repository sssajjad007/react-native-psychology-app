import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  containerContentStyle: {
    alignItems: "center",
    paddingTop: 20,
    paddingBottom: 64,
  },
  buttonContainer: {
    position: "absolute",
    right: 16,
    bottom: 16,
  },
  modal: {
    marginHorizontal: "5%",
  },
});
